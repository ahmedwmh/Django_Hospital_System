# Digital Ocean App Platform Configuration - FIXED VERSION
# This file should be used to deploy directly to Digital Ocean App Platform

name: hospital-system
services:
- name: web
  source_dir: /
  github:
    repo: ahmedwmh/djangoHospital
    branch: main
  run_command: gunicorn hospital_system.wsgi:application --bind 0.0.0.0:8000 --workers 1 --timeout 120 --access-logfile - --error-logfile -
  environment_slug: python
  instance_count: 1
  instance_size_slug: basic-xxs
  http_port: 8000
  routes:
  - path: /
  envs:
  - key: DEBUG
    value: "False"
  - key: SECRET_KEY
    value: "g)t28v^-shs^idfcd5830yb92f)o%rgvooi78a72nvp+e9jow7"
  - key: ALLOWED_HOSTS
    value: "urchin-app-j2low.ondigitalocean.app,*.ondigitalocean.app,localhost,127.0.0.1"
  - key: USE_POSTGRESQL
    value: "False"
  - key: REDIS_URL
    value: ""
  - key: EMAIL_BACKEND
    value: "django.core.mail.backends.console.EmailBackend"
  - key: STATIC_ROOT
    value: "/app/staticfiles"
  - key: MEDIA_ROOT
    value: "/app/media"
  - key: LANGUAGE_CODE
    value: "ar"
  - key: TIME_ZONE
    value: "Asia/Baghdad"

# Note: Using SQLite for simplicity on App Platform
# No database service needed - using SQLite file
